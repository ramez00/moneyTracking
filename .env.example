# Landing page visit alert email (see alerts.py)
# Sent via the Brevo HTTP API (https://api.brevo.com) instead of raw SMTP,
# since PaaS hosts like Render block outbound SMTP ports on their free tier.
ALERT_EMAIL_TO=eng.ramez.mohamed@gmail.com
BREVO_API_KEY=your-brevo-api-key
SENDER_EMAIL=your-verified-brevo-sender@example.com
